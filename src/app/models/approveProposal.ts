//Exporting the approve proposals model

export interface ApproveProposal {
    _id?: string;
    proposalName: string;
    submittedBy: string;
    status: string;
    comments: string;
    submissionDate: string;
}