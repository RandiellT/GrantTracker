//Taken from Jensen (2018)
//Only a few modications were made for this to function.

//Exporting monitor awards model

export interface MonitorAward{
    _id?: string;
    awardName: string;
    fundedBy: string;
    awardedDate: string;
    awardUpdates: string;
}