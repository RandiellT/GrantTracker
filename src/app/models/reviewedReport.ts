//Taken from Jensen (2018)
//Only a few modications were made for this to function.

//Exporting the reviewed reports model

export interface ReviewedReports {
    _id?: string;
    reviewedReports: object;
}