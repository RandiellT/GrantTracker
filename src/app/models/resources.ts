//Taken from Jensen (2018)
//Only a few modications were made for this to function.

//Exporting the interface for the resources model

export interface Resources{
    _id?: string;
    funderUsers: object;
    grantOffers: object;
}